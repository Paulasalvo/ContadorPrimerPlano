<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ContadorPrimerPlano" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.ContadorPrimerPlano" parent="Base.Theme.ContadorPrimerPlano" />
    <!-- Estilo de la barra de la aplicación -->
    <style name="AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <!-- Estilo emergente de la aplicación -->
    <style name="PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>